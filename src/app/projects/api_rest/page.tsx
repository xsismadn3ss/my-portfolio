import type { Metadata } from "next"

export const metadata: Metadata = {
    title: "API REST",
    description: "Ejemplo de una API REST en Next.js"
}

export default function ApiRestPage() {
    return (<></>)
}